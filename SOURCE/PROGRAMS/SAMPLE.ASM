BITS 16

FILE_DATA:
	.FILENAME 				DB 'sample' ;FILE NAME, 6 CHARACTERS
	.EXTENSION				DB 'com' 	;EXTENSION, 3 CHARATCERS

JMP INIT
%INCLUDE "..\IO.INC"
	
INIT:
	MOV AH, 0EH
	MOV AL, 'S'
	INT 10H
	MOV AL, 'A'
	INT 10H
	MOV AL, 'M'
	INT 10H
	MOV AL, 'P'
	INT 10H
	MOV AL, 'L'
	INT 10H
	MOV AL, 'E'
	INT 10H
	MOV AL, 13
	INT 10H
	MOV AL, 10
	INT 10H
	RETF

.EOF			DW 0FF8H
