BITS 16

JMP INIT
NOP

FILE_DATA:
	.FILENAME 				DB 'SAMPLE' ;FILE NAME, 6 CHARACTERS
	.EXTENSION				DB 'COM' 	;EXTENSION, 3 CHARATCERS
	
%INCLUDE "SUB_IO.INC"
%INCLUDE "..\IO.INC"
	
INIT:
	MOV AH, 0EH
	MOV AL, 'S'
	INT 10H
	MOV AL, 'A'
	INT 10H
	MOV AL, 'M'
	INT 10H
	MOV AL, 'P'
	INT 10H
	MOV AL, 'L'
	INT 10H
	MOV AL, 'E'
	INT 10H
	MOV AL, 13
	INT 10H
	MOV AL, 10
	INT 10H
	END_PROGRAM

.EOF			DW 0FF8H

TIMES 512-($-$$) DB 0
